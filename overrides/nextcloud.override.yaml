# overrides/nextcloud.override.yaml
services:
  nextcloud:
    ports: !override []
    volumes:
      - /media/bommel/syncthing:/external-data
    networks:
      - default
      - web

networks:
  web:
    external: true
    name: web
