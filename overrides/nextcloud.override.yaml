# overrides/nextcloud.override.yaml
services:
  nextcloud:
    ports: !override []
    volumes:
      - /media/bommel:/external-data/bommel
      - /media/cotton:/external-data/cotton
    networks:
      - default
      - web

networks:
  web:
    external: true
    name: web
